# PacMan

Pour compiler :

```bash
mkdir build && cd build
cmake -S ../ -B ./
```

Ensuite pour recompiler, depuis le dossier build :

```bash
cmake --build ./
```

Pour lancer le programme :

```bash
./PacMan
```